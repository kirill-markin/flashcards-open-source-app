// After cloning the repository, copy this file to Local.xcconfig.
// Local.xcconfig is gitignored and should never be committed.
// The iOS app expects these values locally, and Xcode Cloud should receive
// the same values through workflow environment variables.
//
// Xcode .xcconfig treats `//` as a comment, so URLs must be written as
// `https:/$()/host` instead of literal `https://host`.
//
// DEVELOPMENT_TEAM is needed for physical-device runs or signed archives.
// APP_BUNDLE_IDENTIFIER can stay aligned with the checked-in default or be
// overridden for personal signing.

// DEVELOPMENT_TEAM = ABCDE12345
APP_BUNDLE_IDENTIFIER = com.flashcards-open-source-app.app
API_BASE_URL = https:/$()/api.nibomo.com/v1
AUTH_BASE_URL = https:/$()/auth.nibomo.com
PRIVACY_POLICY_URL = https:/$()/nibomo.com/privacy/
TERMS_OF_SERVICE_URL = https:/$()/nibomo.com/terms/
SUPPORT_URL = https:/$()/nibomo.com/support/
SUPPORT_EMAIL_ADDRESS = kirill+flashcards@kirill-markin.com
FLASHCARDS_SENTRY_DSN =
FLASHCARDS_SENTRY_ENVIRONMENT = local
FLASHCARDS_SENTRY_TRACES_SAMPLE_RATE = 0.0

// Set the same values in Xcode Cloud:
// XCODE_CLOUD_DEVELOPMENT_TEAM
// XCODE_CLOUD_APP_BUNDLE_IDENTIFIER
// XCODE_CLOUD_API_BASE_URL
// XCODE_CLOUD_AUTH_BASE_URL
// XCODE_CLOUD_PRIVACY_POLICY_URL
// XCODE_CLOUD_TERMS_OF_SERVICE_URL
// XCODE_CLOUD_SUPPORT_URL
// XCODE_CLOUD_SUPPORT_EMAIL_ADDRESS
// XCODE_CLOUD_SENTRY_DSN (secure workflow value; do not commit the real DSN)
// XCODE_CLOUD_SENTRY_ENVIRONMENT = production
// XCODE_CLOUD_SENTRY_TRACES_SAMPLE_RATE = 0.0
// SENTRY_AUTH_TOKEN (secure secret)
// SENTRY_ORG
// SENTRY_IOS_PROJECT
// SENTRY_URL
// SENTRY_CLI_EXPECTED_SHA256 (optional non-secret override when bumping the pinned sentry-cli version)
