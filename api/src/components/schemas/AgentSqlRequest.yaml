type: object
additionalProperties: false
required:
  - sql
properties:
  sql:
    type: string
    description: >-
      One or more statements in the published Flashcards SQL dialect, not full
      PostgreSQL.
