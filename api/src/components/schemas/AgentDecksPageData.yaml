type: object
additionalProperties: false
required:
  - decks
  - nextCursor
properties:
  decks:
    type: array
    items:
      $ref: ./Deck.yaml
  nextCursor:
    anyOf:
      - $ref: ./OpaqueCursor.yaml
      - type: 'null'
