#!/usr/bin/env bash
# Deprecated in v1.
# We do not restrict ALB ingress by Cloudflare IPs in this architecture,
# because v1 has no ALB public entrypoint.

echo "update-cloudflare-ips.sh is deprecated for v1 (no ALB in public path)."
