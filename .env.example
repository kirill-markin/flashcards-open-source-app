# Local Postgres for Docker Compose
MIGRATION_DATABASE_URL=postgresql://flashcards_owner:flashcards_owner@postgres:5432/flashcards
DATABASE_URL=postgresql://app:app@postgres:5432/flashcards
APP_DB_PASSWORD=app
WORKER_DB_PASSWORD=worker

# Optional: local worker schedule timezone
WORKER_TIMEZONE=UTC
