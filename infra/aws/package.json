{
  "name": "flashcards-open-source-app-aws",
  "version": "1.1.2",
  "private": true,
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all",
    "diff": "cdk diff",
    "synth": "cdk synth",
    "test": "npm run build && node --test dist/lambda/**/*.test.js"
  },
  "dependencies": {
    "@aws-crypto/client-node": "^4.2.2",
    "aws-cdk-lib": "^2.248.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "aws-cdk": "^2.1117.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": "24.x"
  }
}
